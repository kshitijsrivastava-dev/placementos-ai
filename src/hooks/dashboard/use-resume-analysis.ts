import { RESUME_ANALYSIS } from "@/data/resume-mock";

/** Resume analysis data — replace mock imports with API fetch logic when backend is ready. */
export function useResumeAnalysis() {
  return {
    analysis: RESUME_ANALYSIS,
  };
}
